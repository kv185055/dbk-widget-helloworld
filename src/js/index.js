import React from 'react';
import ReactDOM from 'react-dom';
import ClockWidget from './ClockWidget';

const wrapper = document.getElementById('container');

ReactDOM.render(<ClockWidget />, wrapper);
